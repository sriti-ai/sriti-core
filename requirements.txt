fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.2
pydantic>=2.0,<3.0
pydantic-settings>=2.5.2
python-dotenv>=1.0.1

litellm>=1.83.7,<1.88

# Valkey / Redis compatibility
redis>=5.0.8,<6

pyyaml>=6.0.2
numpy>=1.26.0,<2

# Classifier embedding model (ONNX runtime, fast zero-shot inference)
fastembed>=0.3.0

# Prompt compression (LLMLingua-2)
llmlingua>=0.2.0
torch>=2.8.0
transformers>=4.53.0

# Optional: cache encryption at rest
cryptography>=42.0.0

# dev/test only
pytest>=8.0
pytest-asyncio>=0.24
