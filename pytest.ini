[pytest]
pythonpath = .
asyncio_mode = auto
markers =
    integration: slower tests that need a real external dependency (Ollama, Sriti, WhatsApp API) running — skipped by default, run explicitly with `-m integration`.
addopts = -m "not integration"
